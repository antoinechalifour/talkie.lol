import { AudioInputOption, VideoInputOption } from "./types";

export const DEFAULT_AUDIO_OPTION: AudioInputOption = {
  type: "none",
};

export const DEFAULT_VIDEO_OPTION: VideoInputOption = {
  type: "none",
};

export const SCREEN_SHARING_OPTION: VideoInputOption = {
  type: "screen",
};
