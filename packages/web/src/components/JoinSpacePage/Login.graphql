mutation Login($slug: String!) {
  login(args: { slug: $slug }) {
    session {
      token
      user {
        id
        name
      }
    }
    rtcConfiguration {
      iceServers {
        urls
      }
    }
  }
}
